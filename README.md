# python_snippets
Python excercises from various sources

## Dependencies

To install project dependencies:

- Using pip (recommended in a virtual environment):
  pip install -r requirements.txt

This will install the `icecream` library used by icecream.py so you get full ic() functionality.
