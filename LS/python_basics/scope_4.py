a = 1


def my_function():
    print(a)


my_function()

# if we want to only access it, we can but this will work
# but if we wanted to reassigned it, we would need to pass it as an argument to the function or use the global keyword
# otherwise, we would get an error
