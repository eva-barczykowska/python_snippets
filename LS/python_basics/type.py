# How can you check if a variable holds a value that is a list? Given two variables,
# verify whether the values they hold are lists.

object1 = [0, 1, 0, 0, 1]
object2 = 'I leave you my Kingdom, take good care of it.'

print(isinstance(object1, list))
print(isinstance(object2, list))

